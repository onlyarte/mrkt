type Category {
  id: ID! @id
  createdAt: DateTime! @createdAt
  name: String!
  description: String
  parent: Category
}

type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  email: String! @unique
  password: String!
}
